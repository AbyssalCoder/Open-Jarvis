/**
 * Notification toast layer.
 * Renders floating notification toasts in the bottom-left corner.
 */
export function NotificationLayer() {
    // Placeholder — will be wired to notification store
    return <div className="fixed bottom-5 left-5 z-50 pointer-events-none" />;
}
